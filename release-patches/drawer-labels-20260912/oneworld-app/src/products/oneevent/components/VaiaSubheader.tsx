/* CHROME SHIM — the real VAIA subheader is shell-owned and already mounted by <AppShell>.
   The ported pages' own copy renders nothing so the family never shows two VAIA rows. */
export default function VaiaSubheader() { return null; }
