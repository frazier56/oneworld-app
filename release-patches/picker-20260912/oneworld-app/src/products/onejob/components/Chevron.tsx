/**
 * MOVED TO THE SHELL — 11 Aug 2026. See `components/Pickers.tsx` for why.
 *
 * THE chevron (Lee, Jul 26: *"we need to have the same arrows across all of them"*) could not
 * keep being one-app-wide. It is shell chrome now, so the same arrow points in all six.
 * Re-export only.
 */
export { Chevron as default } from "@oneworld/shell";
