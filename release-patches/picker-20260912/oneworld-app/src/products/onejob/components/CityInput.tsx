/**
 * MOVED TO THE SHELL — 11 Aug 2026. See `components/Pickers.tsx` for why.
 * Re-export only. New code imports `CityInput` from `@oneworld/shell`.
 */
export { CityInput as default } from "@oneworld/shell";
